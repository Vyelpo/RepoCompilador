- Detectar el nombre de la clase principal o main
SOL: ?
- identificar el paquete y/o paquetes del cual proviene
SOL: ?
- Eliminar todos los archivos que se generan a la hora de compilar cada codigo
SOL: 
    op1: Los directorios son carpetas, asi que si se puede o enviar los archivos an lugar donde consecuentemente se eliminen del programa estaria bien

    op2: Enviar los archivos creados a la carpeta temporal del sistema C:\Users\(user)\Appdata\Roaming\Temp
    pero si bien se puede hacer esto, entonces podemos enviar esto a una carpeta que se elimine despues una vez finalizada la compilacion
